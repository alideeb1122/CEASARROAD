{
  "name": "@workspace/website",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "node -e \"const { spawn } = require('node:child_process'); const port = process.env.PORT || '3000'; const child = spawn('next', ['dev', '-p', port, '-H', '0.0.0.0'], { stdio: 'inherit', shell: true }); child.on('exit', (code) => process.exit(code ?? 0));\"",
    "build": "next build && node scripts/prepare-static-export.mjs",
    "start": "node -e \"const { spawn } = require('node:child_process'); const port = process.env.PORT || '3000'; const child = spawn('next', ['start', '-p', port, '-H', '0.0.0.0'], { stdio: 'inherit', shell: true }); child.on('exit', (code) => process.exit(code ?? 0));\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "lucide-react": "^1.18.0",
    "next": "15.2.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tailwind-merge": "^3.6.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "autoprefixer": "^10.0.1",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}
